# Runtime dependencies for O-Banking.
#
# These are the only third-party packages the application actually imports:
#   Django               - the web framework (LTS)
#   django-jazzmin       - admin theme, listed first in INSTALLED_APPS
#   django-import-export - ImportExportModelAdmin in account/admin.py
#   shortuuid            - ShortUUIDField for account and transaction identifiers
#   Pillow               - ImageField support for the KYC uploads
#   pyotp                - TOTP verification for two-factor authentication
#   qrcode[pil]          - PNG QR code for the 2FA enrolment screen
#
# Pinned to the versions this project is verified against. The previous list
# carried roughly 43 packages that no code referenced: the whole DRF / djoser /
# simplejwt / rest-auth set, CKEditor / TinyMCE / taggit / crispy-forms /
# widget-tweaks, and an aspirational Heroku + S3 + PostgreSQL staging
# (gunicorn, django-heroku, dj-database-url, psycopg2, whitenoise, boto3,
# django-storages). django-shortuuidfield is also gone: the code imports
# shortuuid.django_fields, which comes from shortuuid itself.
Django==5.2.17
django-jazzmin==3.0.5
django-import-export==4.4.1
shortuuid==1.0.13
Pillow==12.3.0
pyotp==2.9.0
qrcode[pil]==7.4.2